import { Text, View } from 'react-native';

export default function Active() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <Text>active bingo screen.</Text>
    </View>
  );
}
// TODO:
//  2. Bingo Screen - active bingo if not button create bingo
